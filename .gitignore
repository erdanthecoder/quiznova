__pycache__/
*.pyc
data/
.DS_Store

# Firebase service-account keys are private: they must never reach a public repo
*firebase-adminsdk*.json
*service-account*.json
serviceAccountKey.json
.firebase/

# the desktop app's own dependencies and builds
desktop/node_modules/
desktop/dist/
desktop/out/

# built app binaries: they are made by the Windows workflow, not committed
desktop/dist/
